<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oerlex.as223th_assignment1">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RandomNumber"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BMI"
            android:parentActivityName=".RandomNumber" />
        <activity
            android:name=".countryList.CountryList"
            android:parentActivityName=".RandomNumber" />
        <activity
            android:name=".countryList.CountryInput"
            android:parentActivityName=".countryList.CountryList" />
        <activity
            android:name=".weather.VaxjoWeather"
            android:parentActivityName=".RandomNumber" />
        <activity
            android:name=".beer.BeerFragment"
            android:parentActivityName=".RandomNumber" />

    </application>

</manifest>